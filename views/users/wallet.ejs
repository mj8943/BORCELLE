<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BORCELLE</title>

    <link rel="icon" href="/Favicon.png" type="image/x-icon">

    <style>
        .lab {
            color: white;
        }

        .profile {
            margin-top: 20px;
            width: 120px;
            margin-left: 700px;
            color: antiquewhite;
            background-color: rgb(112, 7, 7);
            border-color: antiquewhite;
            height: 30px;
        }

        .history table {
            width: 100%;
            border-collapse: collapse;
        }

        .history td {
            border-bottom: 1px solid #ddd;
            padding: 8px;
            text-align: center;
            padding-bottom: 10px;
        }

        .history tr {
            background-color: antiquewhite;
        }

       
    </style>
</head>

<body>
    <div style="background-color: rgb(112, 7, 7); display:flex; padding: 60px; padding-left: 300px; margin-top: 100px;">
        <div>
            <img src="borcelle real.png" alt="logo" style="height: 250px;">
        </div>
        <div style="margin-left: 50px; margin-top: 50px;">
            <h3 style="color: antiquewhite;">USER WALLET</h3>
            <h3 class="lab">
                <%= wallet.userId.name %>
            </h3>
            <h4 style=" background-color: antiquewhite; color: rgb(112, 7, 7); width: 180px; height: 20px;">Available
                Balance:&nbsp;&#8377;<%= wallet.balance %>
            </h4>
        </div>
        <div class="history" style="margin-left: 30px; width: 700px;">
            <% if (wallet.history.length > 0) { %>
                <table>
                    <tbody>
                        <% wallet.history.slice(0, 5).forEach(transaction=> { %>
                            <tr>
                                <td class="<%= transaction.transaction.toLowerCase() %>" style="font-weight: 600;">
                                    <% if (transaction.transaction === 'Credited') { %>
                                        + &#8377;<%= transaction.amount %>
                                    <% } else { %>
                                        - &#8377;<%= transaction.amount %>
                                    <% } %>
                                </td>
                                <td style="font-weight: 600;">
                                    <%= transaction.date.toLocaleDateString() %>
                                </td>
                                <td style="font-weight: 600;">
                                    <%= transaction.Reason %>
                                </td>
                                <% if(transaction.transaction == "Credited"){ %>
                                  <td style="color: green; font-weight: 600;">
                                    <%= transaction.transaction %>
                                  </td>
                               <% }else{ %>
                                <td style="color: red; font-weight: 600;">
                                    <%= transaction.transaction %>
                                  </td>
                                <% } %>
                                
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <p style="color: #ddd;">No transaction history available.</p>
            <% } %>
        </div>
    </div>

    <a href="/user-profile"><button class="profile">Back to Profile</button></a>
</body>

</html>
